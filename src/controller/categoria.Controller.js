const {request, response} = require('express'); 
const Categoria = require('../model/categoria');

async function categoriaMenu( req= request, res = response){
    
    
}

module.exports ={
    categoriaMenu
}